# Backend API origin for Vite proxy (optional).
# Local dev: backend at http://localhost:8000
# Docker: use http://backend:8000 so the frontend container can reach the backend.
# VITE_API_ORIGIN=http://backend:8000
